// Service pour générer des sélections aléatoires simples et multiples
// Étapes :

// Créer les fonctions utilitaires :

// getRandomItem(array) : sélectionne un élément aléatoire
// getRandomItems(array, count) : sélectionne plusieurs éléments
// getRandomNumber(min, max) : génère un nombre aléatoire
// getRandomBudget() : génère un budget entre 500 et 1,000,000


// Créer la fonction principale generateChallenge(options)
// Exporter toutes les fonctions

// Fonctions à implémenter :

// Logique pour gérer les packs sélectionnés
// Logique pour les options (couleur, pièces, étages, famille, timer)
// Logique pour les restrictions
// Génération des résultats finaux
